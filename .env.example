# Copy to .env and set as needed. Used by docker compose and CLI/TUI/backend.
# Default: devnet (deployment proof). For local validator use http://127.0.0.1:8899
RPC_URL=https://api.devnet.solana.com
# KEYPAIR_PATH=/path/to/keypair.json
# Or base64-encoded keypair array: KEYPAIR_JSON=WyJ...]
# MINT_ADDRESS=...  # default mint for API requests
